interface:
  display_name: "KW-OKF Memory"
  short_description: "Turn Codex work into an audited local memory vault"
  default_prompt: "Use $kw-okf-memory to save or retrieve durable project memory."

policy:
  allow_implicit_invocation: true
